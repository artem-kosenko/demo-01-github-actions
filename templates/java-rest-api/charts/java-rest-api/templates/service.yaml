apiVersion: v1
kind: Service
metadata:
  name: {{ include "java-rest-api.fullname" . }}
  labels:
    {{- include "java-rest-api.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "java-rest-api.selectorLabels" . | nindent 4 }}
