apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "java-rest-api.fullname" . }}-test-connection"
  labels:
    {{- include "java-rest-api.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "java-rest-api.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
