name: Java REST API Helm Chart CI

on:
  push:
    branches:
      - '**'
    paths:
      - 'templates/java-rest-api/charts/**'
  pull_request:
    paths:
      - 'templates/java-rest-api/charts/**'

jobs:
  lint:
    runs-on: self-hosted
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Helm
        uses: azure/setup-helm@v4
        with:
          version: v3.15.2

      - name: Helm lint
        run: helm lint templates/java-rest-api/charts/java-rest-api

      - name: Helm template render
        run: helm template java-rest-api templates/java-rest-api/charts/java-rest-api --debug
